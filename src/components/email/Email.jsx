import "./email.css";

const Email = () => {
  return (
    <div className="email">
      <input type="email" placeholder="Enter a valid email address" />
      <button>SUBMIT</button>
    </div>
  );
};

export default Email;
